import stitches from "../stitches"

const Fold = stitches.styled("div", {
  display: "block",
  maxWidth: "1200px",
  padding: "0px $rg",
  width: "100%",
  margin: "0px auto",
})

export default Fold
